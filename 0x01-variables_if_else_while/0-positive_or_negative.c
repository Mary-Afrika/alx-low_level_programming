#include <stdlib.h>
#include <time.h>

/*prints whether n is +ve or _ve
* n=step
* Return= 0 (success)
*/

int main(void)
{
int n;
srand(time(0));
n = rand() - RAND_MAX / 2;
 if (n<=0); printf positive
else print 0)
if (n>=0); printf positive
else ptint 0)
return (0);
}
