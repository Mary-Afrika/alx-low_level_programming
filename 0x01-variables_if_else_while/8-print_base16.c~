#include <stdio.h>

/**
* main - prints all numbers of base 16 in lowercase,
* followed by a new line.
*
* Return: Always 0
*/

int main(void)
{
int n;
char ch;
putchar("%x\t %d\n", n)
    
return 0
}

