#include <stdio.h>

/**
* main - prints all strings from 0 to 10
* followed by a new line
*
* Return: Always 0
*/

int main(void)
{
int n;

for (n = 0; n <= 10; n++)
putchar(n);

putchar('\n');

return (0);
}
